name: Geopolitical Risk Collection & AI Report

on:
  schedule:
    # 毎日 UTC 0:00 と 12:00 に実行（JST 9:00 と 21:00）
    - cron: '0 0 * * *'
    - cron: '0 12 * * *'
  workflow_dispatch:  # 手動実行も可能

permissions:
  contents: write

jobs:
  collect-and-publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Collect geopolitical news
        run: python scripts/collector.py --type geopolitical
        env:
          PYTHONPATH: scripts

      - name: Generate geopolitical report
        run: python scripts/ai_writer.py --type geopolitical
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PYTHONPATH: scripts

      - name: Collect financial news
        run: python scripts/collector.py --type financial
        env:
          PYTHONPATH: scripts

      - name: Collect market data
        run: python scripts/market_data_collector.py
        env:
          PYTHONPATH: scripts

      - name: Generate financial report
        run: python scripts/ai_writer.py --type financial
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PYTHONPATH: scripts

      - name: Collect and analyze influential statements
        run: |
          python scripts/collector.py --type statements
          python scripts/ai_writer.py --type statements
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PYTHONPATH: scripts

      - name: Collect fuel inventory data
        run: python scripts/fuel_data_collector.py
        env:
          PYTHONPATH: scripts

      - name: Collect and analyze combat logs
        run: python scripts/combat_log_collector.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PYTHONPATH: scripts

      - name: Collect and analyze panic situations
        run: python scripts/panic_collector.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PYTHONPATH: scripts

      - name: Commit and push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/
          git diff --staged --quiet || (git commit -m "📊 Update geopolitical risk report - $(date -u '+%Y-%m-%d %H:%M UTC')" && git push)
